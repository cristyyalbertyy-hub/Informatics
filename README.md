# Information Processing

Static learning app for Information Processing classes, with videos, podcasts,
infographics, and CSV-based questions.

## Local use

On Windows, run:

```bat
start-app.bat
```

Then open `http://localhost:5500`.

Questions require the local server because browsers block CSV loading when the
page is opened directly from the file system.
